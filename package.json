{
  "name": "deployable-errors",
  "version": "0.9.1",
  "description": "Custom Error classes",
  "repository": {
    "type": "git",
    "url": "https://github.com/deployable/node-deployable-errors.git"
  },
  "main": "lib/errors.js",
  "scripts": {
    "test": "mocha",
    "test:int": "mocha test/int_*",
    "test:unit": "mocha test/unit_*",
    "test:coverage": "istanbul cover _mocha && istanbul check-coverage",
    "release:patch": "set -uex; npm run test:coverage && npm version patch && git push && npm publish && git push --tags",
    "release:test": "set -uex; npm run test; npm version prerelease; npm publish --tag testing"
  },
  "keywords": [
    "error",
    "deployable"
  ],
  "author": "matt at deployable.co",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "chai": "^3.5.0",
    "coffee-script": "~1.12.0",
    "istanbul": "^0.4.5",
    "mocha": "^3.2.0"
  }
}
