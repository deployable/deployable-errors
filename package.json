{
  "name": "deployable-errors",
  "version": "0.5.3",
  "description": "Custom Error classes",
  "repository": {
    "type": "git",
    "url": "https://github.com/deployable/node-deployable-errors.git"
  },
  "main": "lib/errors.js",
  "scripts": {
    "test": "mocha",
    "test:int": "mocha test/int_*",
    "test:unit": "mocha test/unit_*",
    "test:coverage": "istanbul cover _mocha",
    "npm:publish": "set -uex && npm run test:coverage && [ -z \"$(git status --porcelain)\" ] && npm version patch && git push && git push --tags && npm publish"
  },
  "keywords": [
    "error",
    "deployable"
  ],
  "author": "matt at deployable.co",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "chai": "^3.5.0",
    "coffee-script": "~1.12.0",
    "istanbul": "^0.4.5",
    "mocha": "^3.2.0"
  }
}
